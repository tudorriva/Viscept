version: '3.8'

services:
  # Backend Express server
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: viscept-backend
    ports:
      - "3001:3001"
    environment:
      - OLLAMA_URL=${OLLAMA_URL:-http://host.docker.internal:11434}
      - OLLAMA_MODEL=${OLLAMA_MODEL:-qwen2.5-coder:7b}
      - OLLAMA_VLM_MODEL=${OLLAMA_VLM_MODEL:-qwen2.5-vl:3b}
      - MAX_VALIDATION_RETRIES=${MAX_VALIDATION_RETRIES:-2}
      - MODEL_COOLDOWN_MS=${MODEL_COOLDOWN_MS:-2000}
      - VLM_TIMEOUT=${VLM_TIMEOUT:-120000}
      - NODE_ENV=${NODE_ENV:-development}
      - PORT=3001
    networks:
      - diagram-network
    restart: unless-stopped

  # Frontend Vite + React app
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: viscept-frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=${FRONTEND_API_URL:-http://backend:3001}
    networks:
      - diagram-network
    restart: unless-stopped

  # PlantUML Server
  plantuml:
    image: plantuml/plantuml-server:jetty
    container_name: viscept-plantuml
    ports:
      - "8080:8080"
    environment:
      - PLANTUML_SECURITY_ALLOW_FETCH=true
    networks:
      - diagram-network
    restart: unless-stopped
    profiles:
      - plantuml

networks:
  diagram-network:
    driver: bridge